import {StyleSheet} from 'react-native';
import {colors} from '../../ui';

export const styles = StyleSheet.create({
  container: {
    height: 20,
    marginVertical: 3,
  },
  errorText: {
    color: colors.red,
  },
});
